---
title: AlertDialog
category: Components
package: "@spark-ui/alert-dialog"
---

export const data = docgen['alert-dialog'] || {}

<ComponentMenu slug="alert-dialog" />

## AlertDialog

Contains all the parts of an alert dialog.

<ComponentProps docgen={data["AlertDialog"]} />

## AlertDialog.Trigger

A button that opens the dialog.

<ComponentProps docgen={data["AlertDialog.Trigger"]} />

## AlertDialog.Portal

When used, portals your overlay and content parts into the body.

<ComponentProps docgen={data["AlertDialog.Portal"]} />

## AlertDialog.Overlay

A layer that covers the inert portion of the view when the dialog is open.

<ComponentProps docgen={data["AlertDialog.Overlay"]} />

## AlertDialog.Content

Contains content to be rendered when the dialog is open.

<ComponentProps docgen={data["AlertDialog.Content"]} />

## AlertDialog.Header

To use inside of AlertDialog.Content. Sticky header inside the dialog.

<ComponentProps docgen={data["AlertDialog.Header"]} />

## AlertDialog.Body

To use inside of AlertDialog.Content. Main content of the dialog. Becomes scrollable when content is overflowing the screen.

<ComponentProps docgen={data["AlertDialog.Body"]} />

## AlertDialog.Footer

To use inside of AlertDialog.Content. Sticky footer to be rendered in the open dialog. Use it to render CTAs and make them accessible in every condition.

<ComponentProps docgen={data["AlertDialog.Footer"]} />

## AlertDialog.Title

An accessible name to be announced when the dialog is opened.

<ComponentProps docgen={data["AlertDialog.Title"]} />

## AlertDialog.Description

An accessible description to be announced when the dialog is opened.

<ComponentProps docgen={data["AlertDialog.Description"]} />

## AlertDialog.Cancel

A button that closes the dialog. This button should be distinguished visually from AlertDialog.Action buttons.

<ComponentProps docgen={data["AlertDialog.Cancel"]} />

## AlertDialog.Action

A button that closes the dialog. These buttons should be distinguished visually from the AlertDialog.Cancel button.

<ComponentProps docgen={data["AlertDialog.Action"]} />
